apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kutt.fullname" . }}-test-connection"
  labels:
    {{- include "kutt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['--spider', '{{ include "kutt.fullname" . }}:{{ .Values.service.port }}/api/v2/health']
  restartPolicy: Never
